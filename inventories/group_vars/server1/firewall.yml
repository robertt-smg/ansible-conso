## Role: geerlingguy.firewall
## -------------------------

server1_firewall_allowed_tcp_ports:
  - "22" # ssh port, pre bootstrap
  - "25" # this is only accessible from inside networks (podman), as ionos has a firewall for world connections, to proctect us
  - "443" # web server, ha proxy
  - "31222" # modfied ssh port, after bootstrap
  - "4646" # nomad ui, protected by access token
  
