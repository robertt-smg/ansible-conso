- hosts: Linux
  become: true
  gather_facts: false
  tasks:
    - name: Install Python
      raw: |
        apt-get update
        apt-get install -y python3 python3-pymysql python3-pip python3-cryptography # For Debian/Ubuntu
