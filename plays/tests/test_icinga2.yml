- hosts: all
  #become: true
  become_method: runas
  become_user: Administrator
  gather_facts: false

  vars_files:
  - main/vars.yml

  tasks:

  - name: Install icinga2
    include_tasks: ../win-tasks/icinga2.yml
