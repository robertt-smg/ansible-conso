- hosts: all
  become: false
  become_method: runas
  become_user: Administrator
  gather_facts: false

  vars_files:
  - main/vars.yml

  tasks:
  - name: Ping all hosts
    win_ping:
      data: "Hostname: {{ inventory_hostname }}"