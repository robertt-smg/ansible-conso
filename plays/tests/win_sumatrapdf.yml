- hosts: all
  become: true
  become_method: runas
  become_user: Administrator
  gather_facts: false

  vars_files:
  - main/vars.yml

  tasks:

    - name: Install SumatraPDF
      include_tasks: ../win-tasks/sumatrapdf.yml
      when: airquest_defaults.enabled is defined and airquest_defaults.enabled
